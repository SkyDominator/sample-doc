# nano-llm-engine SDK

Python SDK for mock LLM inference runtime used in the docs-as-code portfolio.

## Features

- Mock hardware load/unload lifecycle
- Synchronous and streaming generation APIs
- Optional Rust-backed device memory helper (`nano_llm_rs`)
