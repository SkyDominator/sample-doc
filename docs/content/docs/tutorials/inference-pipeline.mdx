---
title: 추론 파이프라인 튜토리얼
description: 엔드투엔드 추론 파이프라인 예제
---

# 추론 파이프라인

이 튜토리얼은 `NanoLLMEngine` 인스턴스를 초기화한 뒤, 생성 요청과 스트리밍 요청을 실행하고, 메모리 상태와 오류 경로를 점검한 뒤 세션을 종료하는 흐름을 따라갑니다.

## 1) 런타임 초기화

```python
import os

from nano_llm import InferenceConfig, KVCacheConfig, NanoLLMEngine

os.environ["MOCK_RNGD_HARDWARE"] = "true"

engine = NanoLLMEngine(
    model_path="models/mock-llm.bin",
    quantization="INT8",
    kv_cache_config=KVCacheConfig(max_seq_len=8192, cache_dtype="fp16"),
)
engine.load_to_device(device_id=0)
```

## 2) 단건 생성 요청

```python
import os

from nano_llm import InferenceConfig, NanoLLMEngine

os.environ["MOCK_RNGD_HARDWARE"] = "true"

engine = NanoLLMEngine(model_path="models/mock-llm.bin", quantization="INT8")
engine.load_to_device(device_id=0)

cfg = InferenceConfig(batch_size=1, max_tokens=80, temperature=0.2)
out = engine.generate("INT8 설정이 메모리 사용량 표기에 어떤 의미를 갖는지 설명해줘", cfg)
print(out)

engine.unload()
```

`generate()`는 `[mock:{quantization}:tokens={max_tokens}]` 접두사를 붙여 현재 엔진/요청 설정 중 일부를 결과 문자열에서 바로 확인하게 합니다.

## 3) 스트리밍 요청

```python
import os

from nano_llm import InferenceConfig, NanoLLMEngine

os.environ["MOCK_RNGD_HARDWARE"] = "true"

engine = NanoLLMEngine(model_path="models/mock-llm.bin", quantization="INT8")
engine.load_to_device(device_id=0)

for token in engine.generate_streaming(
	"배치 추론 설정이 스트리밍 출력에 어떻게 반영되는지 설명해줘",
    InferenceConfig(max_tokens=40, temperature=0.1),
):
    print(token)

engine.unload()
```

`generate_streaming()`은 내부에서 `generate()` 결과를 재사용하므로, 토큰을 다시 이어 붙이면 동기 호출과 같은 payload를 얻습니다.

## 4) 메모리 텔레메트리 확인

```python
import os

from nano_llm import NanoLLMEngine

os.environ["MOCK_RNGD_HARDWARE"] = "true"

engine = NanoLLMEngine(model_path="models/mock-llm.bin")
print(engine.get_device_memory())

engine.load_to_device(0)
stats = engine.get_device_memory()
print(stats["total_mb"], stats["used_mb"])

engine.unload()
```

`get_device_memory()`는 엔진이 현재 점유 중인 mock 메모리 상태를 읽습니다. 로드 전에는 `used_mb`가 `0`, 로드 후에는 `1024`입니다.

## 5) 장치 메모리 풀 예약 확인

```python
from nano_llm import DeviceMemory

memory = DeviceMemory(total_bytes=32 * 1024 * 1024)
reserved = memory.allocate(16 * 1024 * 1024)
print(reserved, f"{memory.utilization():.2f}")

memory.free(16 * 1024 * 1024)
print(f"{memory.utilization():.2f}")
```

이 단계는 엔진 텔레메트리와 별개로, 독립적인 메모리 풀에서 예약 가능 여부를 확인하는 패턴을 보여줍니다.

## 6) 오류 처리 패턴

```python
import os

from nano_llm import NanoLLMEngine
from nano_llm.errors import DeviceNotFoundError

os.environ.pop("MOCK_RNGD_HARDWARE", None)

probe = NanoLLMEngine(model_path="models/mock-llm.bin")
try:
    probe.load_to_device(0)
except DeviceNotFoundError as exc:
    print(f"예상 가능한 오류: {exc}")
```

## 7) 종료

```python
import os

from nano_llm import NanoLLMEngine

os.environ["MOCK_RNGD_HARDWARE"] = "true"

engine = NanoLLMEngine(model_path="models/mock-llm.bin")
engine.load_to_device(0)
engine.unload()
```

## 운영 체크리스트

1. `MOCK_RNGD_HARDWARE` 없이 `load_to_device()`를 호출하면 `DeviceNotFoundError`가 발생하는가?
2. 로드 후 `generate()` 결과에 현재 `quantization`과 `max_tokens` 값이 반영되는가?
3. `generate_streaming()` 토큰을 다시 합치면 동기 결과와 같은 문자열이 되는가?
4. `unload()` 후 `is_loaded`가 `False`이고 `get_device_memory()["used_mb"]`가 `0`인가?
