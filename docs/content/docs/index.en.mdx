---
title: RayKimLLM
description: Overview of the RayKimLLM codebase
---

# RayKimLLM

`RayKimLLM` is a compact example codebase that models a mock LLM inference path.
Its core pieces are the Python SDK's engine lifecycle, request configuration objects, device memory abstraction, and tests that verify the behavior.

## Core components

- `RayKimLLMEngine`: handles device loading, synchronous generation, streaming generation, memory queries, and unload.
- `InferenceConfig`: stores request-scoped settings such as batch size, token budget, and temperature.
- `KVCacheConfig`: keeps KV cache length and precision labels as engine-level configuration.
- `DeviceMemory`: a memory-pool wrapper that uses the Rust extension when available and falls back to a Python implementation otherwise.

## Getting started

<Cards>
	<Card>
		<CardHeader>
			<CardTitle><a href="/en/docs/guides/quickstart">Quickstart</a></CardTitle>
			<CardDescription>Open a mock device session, run one request, and inspect the output contract.</CardDescription>
		</CardHeader>
	</Card>
	<Card>
		<CardHeader>
			<CardTitle><a href="/en/docs/guides/concepts">Core Concepts</a></CardTitle>
			<CardDescription>See how quantization labels, KV cache settings, and request config fit into the mock runtime.</CardDescription>
		</CardHeader>
	</Card>
	<Card>
		<CardHeader>
			<CardTitle><a href="/en/docs/api/engine">API Reference</a></CardTitle>
			<CardDescription>Jump straight to constructor signatures, method contracts, return types, and exceptions.</CardDescription>
		</CardHeader>
	</Card>
	<Card>
		<CardHeader>
			<CardTitle><a href="/en/docs/tutorials/inference-pipeline">Inference Pipeline</a></CardTitle>
			<CardDescription>Follow the end-to-end request flow from engine creation to unload with concrete checkpoints.</CardDescription>
		</CardHeader>
	</Card>
</Cards>

## Current code highlights

<Cards className="xl:grid-cols-3">
	<Card>
		<CardHeader>
			<CardTitle><a href="/en/docs/changes/sidebar-and-shell">Session state and memory telemetry</a></CardTitle>
			<CardDescription>Understand how load state changes the mock device session and the memory numbers returned by the SDK.</CardDescription>
		</CardHeader>
	</Card>
	<Card>
		<CardHeader>
			<CardTitle><a href="/en/docs/changes/code-block-refresh">Generation output metadata added</a></CardTitle>
			<CardDescription>Inspect the fixed output prefix that makes quantization and token budget visible in every generation result.</CardDescription>
		</CardHeader>
	</Card>
	<Card>
		<CardHeader>
			<CardTitle><a href="/en/docs/changes/locale-entrypoints">Explicit configuration object split</a></CardTitle>
			<CardDescription>Review why engine-level settings and request-scoped controls are modeled as separate objects.</CardDescription>
		</CardHeader>
	</Card>
</Cards>
